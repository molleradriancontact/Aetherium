{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the OS Architect application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "username": {
          "type": "string",
          "description": "The username of the user, unique across the platform."
        },
        "registrationDate": {
          "type": "string",
          "description": "Date and time the user registered.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "username",
        "registrationDate"
      ]
    },
    "ArchitectProject": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ArchitectProject",
      "type": "object",
      "description": "Represents a single analysis project for a user in OS Architect.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the project."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this project."
        },
        "name": {
          "type": "string",
          "description": "Name of the project, derived from uploaded files."
        },
        "createdAt": {
          "type": "string",
          "description": "Date and time the project was created.",
          "format": "date-time"
        },
        "analysisReport": {
          "type": "string",
          "description": "The full content of the analysis report."
        },
        "frontendSuggestions": {
          "type": "object",
          "properties": {
            "suggestedChanges": { "type": "string" },
            "reasoning": { "type": "string" }
          }
        },
        "backendSuggestions": {
          "type": "object",
          "properties": {
            "suggestedChanges": { "type": "string" },
            "reasoning": { "type": "string" }
          }
        },
        "history": {
          "type": "array",
          "items": {
            "$ref": "#/entities/HistoryItem"
          }
        },
        "uploadedFiles": {
            "type": "array",
            "description": "A list of files uploaded by the user for analysis.",
            "items": {
                "$ref": "#/entities/UploadedFile"
            }
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "createdAt"
      ]
    },
    "HistoryItem": {
      "title": "HistoryItem",
      "type": "object",
      "description": "A single entry in the project's history log.",
      "properties": {
        "id": {
          "type": "number"
        },
        "message": {
          "type": "string"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["id", "message", "timestamp"]
    },
    "UploadedFile": {
      "title": "UploadedFile",
      "type": "object",
      "description": "Represents a single file uploaded by the user.",
      "properties": {
        "path": {
          "type": "string",
          "description": "The full path of the uploaded file."
        },
        "content": {
          "type": "string",
          "description": "The content of the file, encoded as a data URI."
        }
      },
      "required": ["path", "content"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profile information."
    },
    "/users/{userId}/projects/{projectId}": {
      "schema": {
        "$ref": "#/entities/ArchitectProject"
      },
      "description": "Stores the analysis projects for each user."
    }
  }
}
